# phase 2

copy over the track_sim project from the local machine to the server

configure the ffmpeg streaming environment to stream the track_sim output to youtube

## preperation

copy over the track_sim project from the local machine to the server

confirm virtual environment is set up correctly and all dependencies are installed

setup additional command line commands to start and stop the track_sim output stream

setup additional command line commands to switch the ffmpeg config file from test pattern to track_sim output stream

## tracksim configuration

tracksim will start in infinite mode

will need command line commands to start and stop the track_sim output stream

## ffmpeg configuration

currently the ffmpeg config file is set up to stream a test pattern to youtube.

this needs to be changed to be switched from test parttern to track_sim output via a command line command
